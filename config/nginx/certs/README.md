Place your TLS certificate and key here as:

- `tls.crt`
- `tls.key`

For local testing only, you can generate a self-signed certificate with:

```bash
/home/sean/Personal/dev/Immach/scripts/generate-self-signed-cert.sh
```
