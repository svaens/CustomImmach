# Immich compose settings
UPLOAD_LOCATION=./data/images
DB_DATA_LOCATION=./data/postgres
MODEL_CACHE_LOCATION=./data/model-cache
IMMICH_CONFIG_FILE=/config/immich-config.json
TZ=Europe/Berlin
IMMICH_VERSION=v2
PUBLIC_HOSTNAME=happygolucky-xmg

# Change this before exposing the stack outside your machine.
# Use only A-Za-z0-9 as recommended by Immich.
DB_PASSWORD=ChangeMeImmich123

# These normally do not need to change.
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
